version: "3.9"

services:
  mongo:
    image: mongo:6.0
    container_name: jobeee_mongo
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"

  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: jobeee_api
  #   environment:
  #     - PORT=3000
  #     - NODE_ENV=development
  #     - MONGODB_URI=mongodb://mongo:27017/jobeee
  #     - MONGODB_URI_PRO=mongodb://mongo:27017/jobeee
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - mongo
  #   volumes:
  #     - ./:/usr/src/app
  #     - /usr/src/app/node_modules

  # frontend:
  #   build:
  #     context: ../jobee-frontend
  #     dockerfile: Dockerfile
  #   container_name: jobee_frontend
  #   environment:
  #     - CHOKIDAR_USEPOLLING=true
  #   ports:
  #     - "5173:5173"
  #   command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
  #   depends_on:
  #     - backend
  #   volumes:
  #     - ../jobee-frontend:/usr/src/app
  #     - /usr/src/app/node_modules

volumes:
  mongo-data:
